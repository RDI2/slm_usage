.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install slm_usage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv slm_usage
    $ pip install slm_usage
